function promptPassword() {
    let password = prompt("Enter your password:");
    if (password === "MySecurityPassword") {
        alert("Password accepted! Access granted.");
    } else {
        alert("Incorrect password. Access denied.");
    }
}
